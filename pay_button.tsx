import * as React from \"react\\";\nimport { Button } from \"./registry/dig/components/ui/button\";\n\n// PayButton UI: demonstrates using the MCP Dig 'button' component to create a Pay button.\n// This UI intentionally keeps logic lightweight for a design-system demo.\nexport default function PayButtonUI() {\n  const handlePay = () => {\n    // Demo action; in a real app this would initiate a payment flow.\n    alert(\"Payment started (demo)\");\n  };\n\n  return (\n    <div style={{ padding: 16 }}>\n      {/* Dig Button used as the primary action control for payment */}\n      <Button color=\"primary\" variant=\"default\" size=\"default\" onClick={handlePay}>\n        Pay Now\n      </Button>\n    </div>\n  );\n}\n"}} ,{